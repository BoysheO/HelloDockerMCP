All C# source files are UTF-8 encoded.  
When modifying them, preserve the existing encoding and line endings.  
Both CRLF and LF line endings are acceptable.  
Use explicit UTF-8 file I/O. Do not rewrite files through shell commands that rely on the system default code page.